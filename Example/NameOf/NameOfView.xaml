<UserControl x:Class="Example.NameOfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:nameOfNamespace="clr-namespace:Example.NameOfNamespace"
             d:DataContext="{d:DesignInstance nameOfNamespace:NameOfViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <StackPanel>
        <TextBlock Text="Name" />
        <TextBlock Text="{Binding Name}" Margin="10,0,0,10" />
        <TextBlock Text="NameWithProperty" />
        <TextBlock Text="{Binding NameWithProperty}" Margin="10,0,0,10" />
        <TextBlock Text="Namespace" />
        <TextBlock Text="{Binding Namespace}" Margin="10,0,0,10" />
        <TextBlock Text="FullName" />
        <TextBlock Text="{Binding FullName}" Margin="10,0,0,10" />
        <TextBlock Text="FullNameWithProperty" />
        <TextBlock Text="{Binding FullNameWithProperty}" Margin="10,0,0,0" />
    </StackPanel>

</UserControl>
