<UserControl x:Class="Example.NameOfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:nameOfNamespace="clr-namespace:Example.NameOfNamespace"
             d:DataContext="{d:DesignInstance nameOfNamespace:NameOfViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <StackPanel>
        <TextBlock Text="Name" />
        <TextBlock Margin="10,0,0,10" Text="{Binding Name}" />
        <TextBlock Text="NameWithProperty" />
        <TextBlock Margin="10,0,0,10" Text="{Binding NameWithProperty}" />
        <TextBlock Text="Namespace" />
        <TextBlock Margin="10,0,0,10" Text="{Binding Namespace}" />
        <TextBlock Text="FullName" />
        <TextBlock Margin="10,0,0,10" Text="{Binding FullName}" />
        <TextBlock Text="FullNameWithProperty" />
        <TextBlock Margin="10,0,0,0" Text="{Binding FullNameWithProperty}" />
    </StackPanel>

</UserControl>